# AUTONOMY.md — op42 (signed at kickoff by user goal, 2026-07-24)

Self-decide (no asking): experiment design, parameter/tier choices, discarding
negative results, checkpoint/commit cadence, GPU allocation across the 4 idle
B200s (user granted all), iteration order.

Must stop for a human: shipping to production/PR, changing the objective
triple (gm 1.40 / ≥0.95 / exact / skeleton), relaxing the lineage constraint,
changing the data spec (replicated rows).

Pre-authorized negative conclusion: if after double-locked analysis the 1.40
bar is infeasible under the constraints, say so plainly with numbers and the
per-band decomposition; do not force a win.

Backup discipline (user demand): commit campaign bucket after every iteration
verdict; RESUME_PROMPT.md refreshed every commit.
