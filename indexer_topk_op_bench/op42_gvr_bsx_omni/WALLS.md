# WALLS.md — op42 structural walls
- W1 (inherited, to re-verify on this node): head native-batch arm is
  near-flat BS 1->128 (SM-unsaturated row parallelism) => the BS 2-32 band is
  a LATENCY contest against an already-amortized incumbent; wins there must
  come from fewer passes / lower fixed cost per row, not from amortization.
