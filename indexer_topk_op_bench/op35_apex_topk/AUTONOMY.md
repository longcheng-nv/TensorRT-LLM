# Autonomy contract — op35 (signed by USER prompt 2026-07-16)
- USER directive: "立即着手分析和组建相应的harness,开始持续优化" => full self-decide on
  experiment design, parameters, iteration, discarding negatives, checkpoint cadence,
  node usage on idle b200-038 (8 GPUs, all cool).
- Must stop for human: shipping into TRT-LLM production paths; changing the objective
  triple/envelope; abandoning the campaign.
- Pre-authorized negative conclusion: if the ~50% target is double-locked infeasible on
  a sub-envelope (math floor + relaxed-constraint control), say so plainly with numbers
  and ship the best achieved + per-regime map instead.
- Cost: track GPU-h + tokens in COST.md (USER asked for full cost accounting in the
  final report, priced at official rates).
