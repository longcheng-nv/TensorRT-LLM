# op34 AUTONOMY CONTRACT (signed at kickoff 2026-07-14)
- Self-decide (no asking): parameter/experiment choices, discarding negatives, checkpointing,
  probe-rung design, spending within the implicit campaign budget.
- Must stop for human: shipping to production, changing baseline semantics, changing the
  objective/envelope/30% goal, blowing an explicit cost budget the user sets.
- PRE-AUTHORIZED NEGATIVE CONCLUSION: "if sglang_v2 remains best / the 30% target is double-locked
  infeasible within the GVR skeleton on BS=1, say so plainly with numbers and harvest the
  conditional win region." The agent will NOT force a win and will NOT loop forever on a walled target.
- Loop exit: GOAL met (ship_rule) OR double-locked infeasible (math floor + relaxed-constraint control
  on v4cap) with the regime map + wall attribution reported.
