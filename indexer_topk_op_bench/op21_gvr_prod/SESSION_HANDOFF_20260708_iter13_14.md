# Session handoff — 2026-07-07/08, op21 iter13+iter14 (HLS silicon campaign)

Node: umbriel-b200-028 (both GPUs healthy, 37-38C idle; GPU1 == iter7 axis
-1.3% on the anchor). Branch `omni/op21-gvr-prod`. Everything below is
committed + on shared NFS — a node loss costs NOTHING.

## What this session did (5 commits)

| commit | what |
|---|---|
| 05ad16dabf | [iter12.9] HLS math-validation artifacts from the prior session committed (proto_hls.py, MATH/VALIDATION html, results/proto_hls/) |
| 3203481fb3 | [iter13] **HLS Step 0 + Step 1**: tau(3)=1.219 measured (M=3 added to count_ge_multi_bench); log-falsi fallback SHIPPED default-ON in gvr_ms/gvr_msc (ladder-known bracket counts in s_iscalars[5]/[6], entry+hi-end pass skips, lg2.approx.f32 falsi refine, msc pre-recount skip). nsys A/B: best gm 1.265 (K2048 1M **2.105x**), worst 1.003, real 1.039. alpha silicon-invariant (0.1==0.2). K2048 light-fallback pocket -3..-6% = codegen tax (v3.2 geometry only). |
| 38a1fb79e7 | [iter14] **HLS Step 2**: distributed msc fallback (slice passes + cluster count-merge + push-collect; vendored phase3 prefix contract bypassed). nsys A/B: best gm 1.250 / **worst gm 1.250** (the all_ge geometry falsi couldn't touch) / real 1.059; K2048 1M 1.99x both stress scenarios. **N-GATED default `fb_dist = n >= 524288`** — the ~300-line code mass taxes the msc fast path ~4% SYSTEMATIC (P0 spot 3/5 one-signed) => N<=262144 binaries stay bit-identical to iter13 (P0 verdict untouched). |
| 8f54e1117e | [iter14.1] HLS_VALIDATION_REPORT.html section 6 silicon verdict; section 5 obligations marked RESOLVED |
| 2221abdf4b | [iter14.2] report interactive charts (Plotly+checkbox per op22 conventions) + 135-cell detail tables, generated by scripts/gen_hls_report_charts.py |

**Cumulative HLS silicon result: 1M worst-case tail ~245us -> ~57us; all
exactness gates green every step (incl. NEW op22 stress gate 456/456, run
both with dist forced AND with the final N-gated default).**

## Key falsifications / lessons this session (details in LEARNINGS.md)
- alpha (falsi aim exponent) is silicon-invariant — do NOT re-probe.
- Code mass is a currency: systematic one-signed ~4% fast-path tax from
  code that never executes; the fix is dispatch-gating COMPILATION (n in
  the _compile key), not runtime branches. Sign-consistency across
  independent binaries distinguishes systematic tax from codegen lottery.
- The cluster-barrier red line (iter3/4) is about saved-work/barrier
  ratio: distributing full-row passes wins where distributing us-scale
  serial work lost.
- GVR A/B still compares SORTED index sets (row order nondeterministic).
- Cross-RUN nsys absolutes drift (co-tenancy); only within-run paired
  ratios are verdicts.

## State of the campaign after this session
- HLS: Steps 0/1/2 DONE on silicon. Step 3 (rho-aware placement) stays
  DEFERRED. Open micro-lever: _fb_dist code-mass diet would unlock
  un-gating 65K-262K (~1.25x stress there).
- PR-1 (upstream port): UNCHANGED — port/ FROZEN at iter11 semantics;
  iter13/14 src/ edits make port/assemble_ms.py fail loudly BY DESIGN.
  Log-falsi + dist fallback are post-PR-1 candidate levers.
- Next levers ranked: (a) PR-1 integration on a clean upstream branch
  (PAUSED, user decision); (b) _fb_dist diet; (c) e2e 3-arm A/B + gsm8k.

## MIGRATE to another B200 node (recipe)

Everything is NFS-shared; migration is stateless:

1. On the new node, `cd /home/scratch.loncheng_gpu/workspace/perf/workloads/DSV4/TensorRT-LLM`
   and check `git log --oneline -1` shows `2221abdf4b` (or later) on
   `omni/op21-gvr-prod`.
2. Env: `python3 -c "import torch, cutlass"` must pass (plain python3 on
   umbriel B200 nodes; op21 bench needs ONLY torch+cutlass, NOT trtllm).
3. GPU preflight: `nvidia-smi --query-gpu=index,temperature.gpu
   --format=csv` — idle >50C => do not time on that GPU (019-GPU0 and
   035-GPU0 are known-bad). Check co-tenancy by output-file growth, not
   nvidia-smi (sandbox namespace-blind).
4. **RE-ANCHOR before quoting any absolute number**: run the anchor cell
   `GPU=<healthy>` via `scripts/drive_ab_hls.sh` (it does the anchor
   first) or directly:
   `env -u GITHUB_TOKEN -u HF_TOKEN CUDA_VISIBLE_DEVICES=$GPU nsys profile
   -c cudaProfilerApi --capture-range-end=stop -f true -o /tmp/anchor
   python3 scripts/nsys_run_auto.py 512 fp32 262144 1 60`
   Expected ~17.8+-0.5us on the b200-028/iter7 axis (HEAD has log-falsi ON
   by default; that cell's binary is unchanged by the N-gate). Off by >3%
   => new-silicon axis: full 17-cell grid re-baseline before judging any
   new lever (scripts/drive_nsys_iter2.sh — NOTE it writes results/nsys/
   msa_*, archive the current ones first).
5. Continue work: paste `RESUME_PROMPT.md` (state current through iter14)
   into a fresh session. Gate scripts run from op21_gvr_prod/; the op22
   stress gate runs from op22_temporal_fixed_hr_bench/ (gate_op22.py).
6. Redlines that travel with you: nsys always `env -u GITHUB_TOKEN -u
   HF_TOKEN`; *.sqlite/*.nsys-rep NEVER committed; A/B via the paired
   same-process scripts (scripts/ab_hls_logfalsi.py --env-knob ...), one
   nsys run per scenario; verdicts from scripts/parse_ab_hls.py.

## Artifact map (all relative to op21_gvr_prod/)
- Kernels: src/gvr_ms_op.py (iter13), src/gvr_msc_op.py (iter13+14).
  Knobs: OP21_FB_LOGFALSI / OP21_FB_ALPHA / OP21_FB_DIST (unset = N-rule).
- A/B harness: scripts/{ab_hls_logfalsi.py, drive_ab_hls.sh,
  parse_ab_hls.py, ab_p0_spot_logfalsi.sh} (KNOB/SUFFIX/OUT env-driven).
- Verdict data: results/nsys/iter13_ab_hls{,_a01,_dist}/*.jsonl (+ NFS
  sqlite reps, gitignored), results/nsys/iter1{3,4}_p0_spot/.
- tau(M): ../count_ge_multi_bench/{count_ge_multi.cu, run_m3_sweep.sh,
  results_m3.csv}.
- Docs: ITERATIONS.md iter13/14, LEARNINGS.md, RESUME_PROMPT.md,
  HLS_VALIDATION_REPORT.html (section 6 + interactive 6.6/6.7, regen via
  scripts/gen_hls_report_charts.py), MATH_THRESHOLD_ESTIMATION.html.
